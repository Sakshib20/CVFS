# CVFS
A Customised Virtual File System (CVFS) programmed solely in C to simulate Linux OS internals. It implements core system calls like open, read, write, lseek, and delete. The project provides a custom command-line shell and low-level data structures like the Incore Inode Table and UAREA, delivering a platform-independent file management environment.
